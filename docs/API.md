# API Documentation

## PlagiarismDetector Class

Основной класс для детектирования плагиата в студенческих работах.

### Инициализация

```python
from src.main import PlagiarismDetector

detector = PlagiarismDetector(language='english')
```

**Параметры:**
- `language` (str): Язык для обработки текста. Допустимые значения: `'english'`, `'russian'`. По умолчанию: `'english'`

### Методы

#### load_documents(source, recursive=False)

Загрузка документов из файла, списка файлов или директории.

**Параметры:**
- `source` (str | List[str]): Путь к файлу, список путей или директория
- `recursive` (bool): Рекурсивный поиск в поддиректориях. По умолчанию: `False`

**Возвращает:** `Dict[str, str]` - словарь {имя_файла: содержимое}

**Пример:**
```python
# Загрузка из директории
detector.load_documents('uploads/')

# Загрузка списка файлов
detector.load_documents(['file1.txt', 'file2.txt'])

# Рекурсивная загрузка
detector.load_documents('data/', recursive=True)
```

#### process_documents(remove_stopwords=True, lemmatize=True)

Обработка загруженных документов: токенизация, удаление стоп-слов, лемматизация.

**Параметры:**
- `remove_stopwords` (bool): Удалять ли стоп-слова. По умолчанию: `True`
- `lemmatize` (bool): Применять ли лемматизацию. По умолчанию: `True`

**Возвращает:** `Dict[str, List[str]]` - словарь {имя_файла: список_токенов}

**Пример:**
```python
tokens = detector.process_documents(
    remove_stopwords=True,
    lemmatize=True
)
```

#### compare_documents()

Сравнение всех загруженных документов между собой.

**Возвращает:** `Dict` - словарь с результатами сравнения, содержащий:
- `document_names`: список имен документов
- `cosine_similarity`: матрица cosine similarity
- `sequence_matcher`: матрица sequence matcher
- `bigram_similarity`: матрица bigram similarity
- `average_similarity`: матрица средней схожести

**Пример:**
```python
results = detector.compare_documents()
```

#### compare_two_documents(doc1_name, doc2_name)

Детальное сравнение двух конкретных документов.

**Параметры:**
- `doc1_name` (str): Имя первого документа
- `doc2_name` (str): Имя второго документа

**Возвращает:** `Dict[str, float]` - словарь с метриками схожести:
- `cosine_tfidf`: Cosine similarity с TF-IDF
- `sequence_matcher`: SequenceMatcher ratio
- `lcs_length`: Длина longest common subsequence
- `lcs_percent`: Процент LCS
- `bigram_similarity`: Bigram Jaccard similarity
- `trigram_similarity`: Trigram Jaccard similarity
- `average_similarity`: Средняя схожесть

**Пример:**
```python
details = detector.compare_two_documents('student1.txt', 'student2.txt')
print(f"Cosine similarity: {details['cosine_tfidf']:.2%}")
```

#### visualize_results(save_dir=None, threshold=0.7)

Визуализация результатов сравнения.

**Параметры:**
- `save_dir` (str | None): Директория для сохранения графиков. Если `None`, графики показываются на экране
- `threshold` (float): Порог для определения высокой схожести. По умолчанию: `0.7`

**Пример:**
```python
detector.visualize_results(save_dir='results/', threshold=0.7)
```

#### generate_report(threshold=0.7)

Генерация текстового отчета о плагиате.

**Параметры:**
- `threshold` (float): Порог для определения высокой схожести. По умолчанию: `0.7`

**Возвращает:** `Dict` - словарь с информацией о подозрительных парах:
- `total_documents`: количество документов
- `suspicious_pairs`: список подозрительных пар
- `threshold`: используемый порог

**Пример:**
```python
report = detector.generate_report(threshold=0.6)
print(f"Найдено пар: {len(report['suspicious_pairs'])}")
```

#### print_report(threshold=0.7)

Вывод отчета о плагиате в консоль.

**Параметры:**
- `threshold` (float): Порог для определения высокой схожести. По умолчанию: `0.7`

**Пример:**
```python
detector.print_report(threshold=0.7)
```

---

## Функции

### analyze_plagiarism(source, language='english', threshold=0.7, visualize=True, save_dir=None)

Упрощенная функция для полного анализа плагиата.

**Параметры:**
- `source` (str | List[str]): Путь к файлу, список файлов или директория
- `language` (str): Язык документов. По умолчанию: `'english'`
- `threshold` (float): Порог для определения плагиата. По умолчанию: `0.7`
- `visualize` (bool): Создавать ли визуализации. По умолчанию: `True`
- `save_dir` (str | None): Директория для сохранения результатов

**Возвращает:** `PlagiarismDetector` - объект детектора с результатами

**Пример:**
```python
from src.main import analyze_plagiarism

detector = analyze_plagiarism(
    'uploads/',
    language='english',
    threshold=0.7,
    visualize=True,
    save_dir='results/'
)
```

### analyze_data(data_path)

Простая функция анализа (для совместимости с README).

**Параметры:**
- `data_path` (str): Путь к данным

**Возвращает:** `dict` - результаты анализа

**Пример:**
```python
from src.main import analyze_data

result = analyze_data("uploads/")
print(result)
```

---

## Вспомогательные классы

### TextProcessor

Класс для предварительной обработки текстов.

**Методы:**
- `clean_text(text)`: Очистка текста от URL, email, лишних пробелов
- `tokenize(text)`: Токенизация текста
- `remove_stopwords(tokens)`: Удаление стоп-слов
- `lemmatize(tokens)`: Лемматизация токенов
- `process(text, remove_stopwords=True, lemmatize=True)`: Полная обработка
- `get_ngrams(tokens, n=2)`: Получение n-грамм
- `get_word_frequency(tokens)`: Подсчет частоты слов

### DocumentLoader

Класс для загрузки документов различных форматов.

**Методы:**
- `load(file_path)`: Загрузка файла (автоопределение формата)
- `load_txt(file_path)`: Загрузка .txt файла
- `load_pdf(file_path)`: Загрузка .pdf файла
- `load_docx(file_path)`: Загрузка .docx файла
- `load_multiple(file_paths)`: Загрузка нескольких файлов
- `load_from_directory(directory, recursive=False)`: Загрузка из директории

**Поддерживаемые форматы:** `.txt`, `.pdf`, `.docx`, `.doc`

### SimilarityCalculator

Класс для вычисления схожести между текстами.

**Методы:**
- `cosine_similarity_tfidf(documents)`: Cosine similarity с TF-IDF
- `longest_common_subsequence(text1, text2)`: LCS
- `sequence_matcher_ratio(text1, text2)`: SequenceMatcher
- `ngram_similarity(tokens1, tokens2, n=2)`: N-gram similarity
- `calculate_all_similarities(text1, text2, tokens1, tokens2)`: Все метрики
- `compare_multiple_documents(documents, processed_tokens)`: Множественное сравнение

### SimilarityVisualizer

Класс для визуализации результатов анализа схожести.

**Методы:**
- `plot_similarity_matrix(similarity_matrix, labels, ...)`: Тепловая карта
- `plot_comparison_results(results, doc1_name, doc2_name, ...)`: Сравнение двух документов
- `plot_multiple_matrices(comparison_results, ...)`: Несколько матриц
- `plot_plagiarism_report(comparison_results, threshold, ...)`: Отчет о плагиате

---

## Примеры использования

### Базовый пример

```python
from src.main import analyze_plagiarism

# Простой анализ директории
detector = analyze_plagiarism('uploads/', threshold=0.7)
```

### Расширенный пример

```python
from src.main import PlagiarismDetector

# Создание детектора
detector = PlagiarismDetector(language='english')

# Загрузка документов
detector.load_documents('uploads/')

# Обработка
detector.process_documents(remove_stopwords=True, lemmatize=True)

# Сравнение
results = detector.compare_documents()

# Детальное сравнение двух документов
details = detector.compare_two_documents('doc1.txt', 'doc2.txt')

# Генерация отчета
report = detector.generate_report(threshold=0.7)

# Визуализация
detector.visualize_results(save_dir='results/', threshold=0.7)
```

### Командная строка

```bash
# Базовый анализ
python -m src.main uploads/

# Или через скрипт с примерами
python scripts/example.py
```

---

## Метрики схожести

Система использует несколько методов для определения схожести текстов:

1. **Cosine Similarity (TF-IDF)**: Векторное представление текстов с использованием TF-IDF весов
2. **Sequence Matcher**: Алгоритм сравнения последовательностей из Python's difflib
3. **Longest Common Subsequence (LCS)**: Длина наибольшей общей подпоследовательности
4. **N-gram Similarity**: Jaccard similarity на основе биграмм и триграмм
5. **Average Similarity**: Средняя схожесть по всем метрикам

---

## Интерпретация результатов

### Пороги схожести

- **0.9 - 1.0**: 🔴 Высокий риск плагиата (почти идентичные тексты)
- **0.7 - 0.9**: 🟡 Средний риск (значительная схожесть)
- **0.5 - 0.7**: 🟢 Низкий риск (некоторое сходство)
- **< 0.5**: ✓ Оригинальные тексты

### Рекомендации

- Для академических работ рекомендуется порог **0.6-0.7**
- Для строгого контроля используйте порог **0.5**
- Всегда проверяйте подозрительные пары вручную
